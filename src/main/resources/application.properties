# Application name
spring.application.name=card-management-app

# Primary DataSource (MySQL - for authentication with User table)
spring.datasource.jdbc-url=jdbc:mysql://localhost:3306/card-management-app
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Secondary DataSource (Oracle - for client data, no JPA)
oracle.datasource.jdbc-url=jdbc:oracle:thin:@10.145.48.96:1521/way4db
oracle.datasource.username=int
oracle.datasource.password=way4
oracle.datasource.driver-class-name=oracle.jdbc.OracleDriver

# JPA Configuration (only for MySQL)
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Logging
logging.level.com.api.cardmanagementapp=INFO
logging.level.org.springframework.ws=DEBUG